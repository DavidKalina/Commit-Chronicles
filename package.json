{
  "name": "commit-chronicles",
  "version": "1.0.0",
  "description": "Post commit hook that captures commit, author, and patch level changes",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "bin": {
    "commit-script": "./dist/commitScript.js"
  },
  "scripts": {
    "build": "bun build ./scripts/commitScript.ts --outdir ./dist"
  }
}
